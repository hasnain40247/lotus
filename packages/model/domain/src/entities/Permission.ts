/**
 * Permission entity — re-exported from @gco/schema with no duplication.
 *
 * Includes both `Permission` (request/reply/rule types) and `PermissionSaved`
 * (persisted always/reject decisions stored via IPermissionRepository).
 */
export { Permission, PermissionSaved } from "@gco/schema"
