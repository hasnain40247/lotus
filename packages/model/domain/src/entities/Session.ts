/**
 * Session entity — re-exported from @gco/schema with no duplication.
 *
 * Import from here rather than directly from @gco/schema so that the model
 * layer has a single, stable re-export point.
 */
export { Session } from "@gco/schema"
